## What & why

<!-- What does this change, and which problem does it solve? Link the issue: "Closes #123". -->

## How it was tested

<!-- Tests added or updated, and anything checked by hand in a Filament panel. -->

## Checklist

- [ ] Tests added or updated (`composer test`)
- [ ] Code style applied (`composer lint`) and static analysis clean (`composer analyse`)
- [ ] New or changed strings added to `resources/lang/en/translations.php` (and any locale you speak)
- [ ] `resources/dist/` rebuilt with `npm run build` if a Blade view or a class name changed
- [ ] README updated if the behaviour or the configuration changed
